/**
 * Settings Tab
 * 
 * Renders the SettingsScreen from the settings feature.
 */

import React from 'react';
import { SettingsScreen } from '@/features/settings';

export default function SettingsTab() {
  return <SettingsScreen />;
}

